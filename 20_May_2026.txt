# CONSTRAINTS
CREATE TABLE Person (
    ID int NOT NULL,
    LastName varchar(255) not null,
    FirstName varchar(255) Unique NOT NULL,
    Age int
);

desc person;

ALTER TABLE Person ADD unique(AGE);
ALTER TABLE Person DROP CONSTRAINT firstname;

CREATE TABLE Persons (
    FirstName varchar(255) not null,
    Age int,
    CHECK (Age>=18 and age <=25)
);
insert into persons values ('Shrestha', 20);

Alter table persons add check (Age>=18 and age <=25);

CREATE TABLE Person1 (
    ID int NOT NULL,
    LastName varchar(255) not null,
    FirstName varchar(255) unique NOT NULL,
    Age int,
    City varchar(30) default 'lucknow'
);
desc person1;

CREATE TABLE Person2(
    ID int NOT NULL,
    LastName varchar(255) not null,
    FirstName varchar(255) NOT NULL,
    Age int
);

Alter table person2 add primary key (id);
desc person2;
Alter table person2 drop primary key;

Create table category(
cid int primary key,
cname varchar(100) not null
);

insert into category values (101,'electronics'),(102,'Home App');

select * from category;

CREATE TABLE products (
    pid int primary key,
    pname varchar(100) NOT NULL,
    cid int, 
    FOREIGN KEY (cid) REFERENCES category(cid)
);
desc products;
insert into products values (501,'Power Bank Samsung',101);
insert into products values (502,'Wooden Cupboard',102);
select * from products;
delete from category where cid=101;